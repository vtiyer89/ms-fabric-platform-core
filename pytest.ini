[pytest]
testpaths = tests
# conftest.py is imported directly by test_real_parameter_files.py for its helpers.
# scripts/ is on the path because deploy_fabric_item.py imports resolve_bindings, which imports
# fabric_api — the scripts are loaded by path rather than installed as a package.
pythonpath = tests scripts
addopts = -q
